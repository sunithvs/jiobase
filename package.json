{
  "name": "jiobase",
  "private": true,
  "license": "AGPL-3.0-only",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "ship": "pnpm --filter @jiobase/shared build && pnpm --filter @jiobase/api run deploy && pnpm --filter @jiobase/proxy run deploy && pnpm --filter @jiobase/web run deploy",
    "ship:api": "pnpm --filter @jiobase/shared build && pnpm --filter @jiobase/api run deploy",
    "ship:proxy": "pnpm --filter @jiobase/shared build && pnpm --filter @jiobase/proxy run deploy",
    "ship:web": "pnpm --filter @jiobase/shared build && pnpm --filter @jiobase/web run deploy"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0"
}
