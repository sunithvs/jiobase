{
  "name": "jiobase",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "deploy": "pnpm --filter @jiobase/shared build && pnpm --filter @jiobase/api deploy && pnpm --filter @jiobase/proxy deploy && pnpm --filter @jiobase/web deploy",
    "deploy:api": "pnpm --filter @jiobase/shared build && pnpm --filter @jiobase/api deploy",
    "deploy:proxy": "pnpm --filter @jiobase/shared build && pnpm --filter @jiobase/proxy deploy",
    "deploy:web": "pnpm --filter @jiobase/shared build && pnpm --filter @jiobase/web deploy"
  },
  "devDependencies": {
    "turbo": "^2.4.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0"
}
