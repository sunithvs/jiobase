# No secrets needed for the web app.
# API calls are proxied through SvelteKit in dev (vite.config.ts)
# and through Cloudflare routing in production.

# In production, set the API URL if the API is on a different origin:
# PUBLIC_API_URL=https://api.jiobase.com
